version: "3.9"
services:
  frontend:
    build: ./frontend
    container_name: frontend-container
    ports:
      - 80:80
    restart: always

  backend:
    build: ./backend-expressmongo
    container_name: backend-container
    ports:
      - 8080:8080
    restart: always

